<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:NetHome.Views.Controls" 
             x:DataType="controls:RollerShutterControl"
             x:Class="NetHome.Views.Controls.RollerShutterControl">
    <ContentView.Resources>
        <Style TargetType="Button" x:Key="bottomControlButton">
            <Setter Property="TextColor" Value="White"></Setter>
            <Setter Property="FontSize" Value="16"></Setter>
            <Setter Property="CornerRadius" Value="0"></Setter>
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="{StaticResource Secondary}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="{StaticResource Secondary}" />
                                <Setter Property="TextColor" Value="{StaticResource Neutral}" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentView.Resources>
    <ContentView.Content>
        <Frame x:Name="frame" CornerRadius="15" HasShadow="True" Margin="12,0,12,5" BackgroundColor="{StaticResource Secondary}" Padding="0">
            <StackLayout Spacing="0">
                <Grid ColumnDefinitions="90, *" ColumnSpacing="0" VerticalOptions="Start" HeightRequest="90">
                    <Image Source="roller_shutter.png" Aspect="AspectFit" Margin="20" VerticalOptions="Center" HorizontalOptions="Center"/>
                    <Label Grid.Column="1" Text="{Binding Device.Name}" FontSize="16" VerticalOptions="Center" HorizontalOptions="StartAndExpand"/>
                    <Frame Grid.Column="2" Padding="0" BackgroundColor="Transparent" VerticalOptions="Center" HorizontalOptions="End">
                        <Grid>
                            <Frame IsVisible="{Binding IsWaiting, Converter={StaticResource InvertedBoolConverter}}" Padding="0" Margin="20,20,30,20" BackgroundColor="Transparent" >
                                <Label Text="{Binding Device.CurrentPercentage, StringFormat='{0}%'}" VerticalOptions="Center" HorizontalOptions="Center" FontAttributes="Bold" FontSize="30" />
                            </Frame>
                            <ActivityIndicator IsVisible="{Binding IsWaiting}" Opacity="100" IsRunning="True" 
                                    HorizontalOptions="Center" VerticalOptions="Center" Color="{StaticResource Neutral}" Margin="20"/>
                        </Grid>
                    </Frame>
                </Grid>
                <Grid ColumnSpacing="1" HeightRequest="45" Padding="0,0,0,0" BackgroundColor="{StaticResource Accent}">
                    <Button Text="{Binding Device.FavPos1, StringFormat='{0}%'}" Command="{Binding QuickAction}" CommandParameter="{Binding Device.FavPos1}" IsEnabled="{Binding IsWaiting, Converter={StaticResource InvertedBoolConverter}}" Style="{StaticResource bottomControlButton}" />
                    <Button Text="{Binding Device.FavPos2, StringFormat='{0}%'}" Command="{Binding QuickAction}" CommandParameter="{Binding Device.FavPos2}" IsEnabled="{Binding IsWaiting, Converter={StaticResource InvertedBoolConverter}}" Style="{StaticResource bottomControlButton}" Grid.Column="1" />
                    <Button Text="{Binding Device.FavPos3, StringFormat='{0}%'}" Command="{Binding QuickAction}" CommandParameter="{Binding Device.FavPos3}" IsEnabled="{Binding IsWaiting, Converter={StaticResource InvertedBoolConverter}}" Style="{StaticResource bottomControlButton}" Grid.Column="2" />
                    <Button Text="{Binding Device.FavPos4, StringFormat='{0}%'}" Command="{Binding QuickAction}" CommandParameter="{Binding Device.FavPos4}" IsEnabled="{Binding IsWaiting, Converter={StaticResource InvertedBoolConverter}}" Style="{StaticResource bottomControlButton}" Grid.Column="3" />
                </Grid>
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>